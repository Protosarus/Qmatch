{
  "name": "qmatch-functions",
  "private": true,
  "version": "1.0.0",
  "description": "Trusted backend writers for QMatch (Discover eligibility + Resonance entitlement scaffold).",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "test": "mocha --timeout 10000 test/**/*.test.js",
    "lint": "node -e \"require('./src/discover_eligibility'); require('./src/entitlement_repository'); require('./src/store_verify_apple'); require('./src/store_verify_play'); console.log('ok')\""
  },
  "dependencies": {
    "@apple/app-store-server-library": "^1.6.0",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.3.0"
  },
  "devDependencies": {
    "mocha": "^10.8.2"
  }
}
