{
  "name": "qmatch-functions",
  "private": true,
  "version": "1.0.0",
  "description": "Trusted backend writers for QMatch (Discover eligibility + Resonance entitlement scaffold).",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "test": "mocha --timeout 10000 test/**/*.test.js",
    "lint": "node -e \"require('./src/discover_eligibility'); require('./src/entitlement_repository'); require('./src/store_verify_apple'); require('./src/store_verify_play'); require('./src/canonical_20d_group_normalized_shadow'); require('./src/stage_b2_l2_callable'); require('./src/like_and_maybe_create_match_callable'); require('./src/alignment_signals_batch'); require('./src/list_who_liked_you_callable'); require('./src/send_super_resonance_callable'); require('./src/get_super_resonance_availability_callable'); require('./src/super_resonance_daily_allowance'); require('./src/list_super_resonance_inbox_callable'); require('./src/bounded_map'); require('./src/home_geography'); require('./src/discover_passport_callable'); require('./src/fcm_token_callable'); require('./src/new_message_push'); console.log('ok')\""
  },
  "dependencies": {
    "@apple/app-store-server-library": "^1.6.0",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.3.0",
    "googleapis": "^144.0.0"
  },
  "devDependencies": {
    "mocha": "^10.8.2"
  }
}
