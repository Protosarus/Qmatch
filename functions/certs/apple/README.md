# Apple root certificates (public)

These DER certificates are published by [Apple PKI](https://www.apple.com/certificateauthority/)
and are required by `SignedDataVerifier`.

They are **not** API keys, issuer secrets, or private keys.

Files:
- `AppleIncRootCertificate.cer`
- `AppleRootCA-G3.cer`
